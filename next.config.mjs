/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', "i.pravatar.cc", "randomuser.me"], // Unsplash domain
  },
};

export default nextConfig;
